window.APP_CONFIG = {
    // Không có dấu / ở cuối.
    API_BASE_URL: "https://cloudy-day-mime-requesting.trycloudflare.com",

    // Để trống nếu backend không đặt DEMO_API_KEY.
    // Đây là khóa demo hiển thị công khai trong mã frontend, không phải bí mật thực sự.
    DEMO_API_KEY: ""
};
